# AIMeter environment overrides
# Priority: environment variables > config.yaml > built-in defaults
# All variables are optional unless noted.
#
# Config file loading
# Allowed:
# - relative file path: resolved from the project root
# - absolute file path: used as-is
# Default when unset:
# - use ./config.yaml if that file exists
# - otherwise no config file is loaded
# Example:
# AIMETER_CONFIG_FILE=./config.yaml

# API base path or absolute API URL.
# Allowed:
# - URL path, for example /api: use the same host as the frontend
# - absolute URL, for example https://api.example.com: call a separate backend origin
# Overrides config.yaml: server.apiUrl
# Built-in default when unset in both env and config.yaml: /api
AIMETER_API_URL=/api

# Vite dev server port for local development.
# Allowed:
# - any positive integer port, for example 3000
# Overrides config.yaml: server.frontendPort
# Built-in default when unset in both env and config.yaml: 3000
AIMETER_FRONTEND_PORT=3000

# Server
# Backend HTTP server listen port.
# Allowed:
# - any positive integer port, for example 3001
# Overrides config.yaml: server.backendPort
# Built-in default when unset in both env and config.yaml: 3001
AIMETER_BACKEND_PORT=3001

# Allowed CORS origins for browser requests.
# Allowed:
# - single origin, for example http://localhost:3000
# - comma-separated origin list, for example http://localhost:3000,https://your-domain.com
# Overrides config.yaml: server.corsOrigins
# Built-in default when unset in both env and config.yaml: empty (no custom CORS middleware)
# Example:
# AIMETER_CORS_ORIGIN=http://localhost:3000,https://your-domain.com

# Whether to trust reverse-proxy forwarded headers.
# Allowed:
# - true: trust x-forwarded-* headers (recommended behind nginx/ingress)
# - false: ignore proxy headers
# Built-in default when unset in both env and config.yaml:
# - true in production
# - false in non-production
# Example:
# AIMETER_TRUST_PROXY=true

# Runtime
# Switch all provider data fetching to mock mode.
# Allowed:
# - true: all provider usage comes from mock data
# - false: all provider usage comes from live adapters
# Built-in default when unset in both env and config.yaml: false
AIMETER_MOCK_ENABLED=false

# Select the process runtime model.
# Allowed:
# - node: enable in-process scheduler (node-cron)
# - serverless: disable in-process scheduler; refresh is request-triggered only
# Built-in default when unset in both env and config.yaml: node
AIMETER_RUNTIME_MODE=node

# Database
# Enable or disable database-backed mode.
# Allowed:
# - true: provider config comes from database; history is persisted
# - false: provider config comes from config.yaml or provider env vars; history is disabled
# Built-in default when unset in both env and config.yaml: true
AIMETER_DATABASE_ENABLED=true

# Select database engine.
# Allowed:
# - sqlite: use a local SQLite database file
# - postgres: use a PostgreSQL database via DSN connection string
# - mysql: use a MySQL database via DSN connection string
# Built-in default when unset in both env and config.yaml: sqlite
AIMETER_DATABASE_ENGINE=sqlite

# Main database connection string.
# Allowed:
# - sqlite file path, for example ./data/aimeter.db: store data in a local SQLite file
# - postgres DSN, for example postgresql://postgres:password@127.0.0.1:5432/aimeter
# - mysql DSN, for example mysql://root:password@127.0.0.1:3306/aimeter
# Built-in default when unset in both env and config.yaml: ./data/aimeter.db
AIMETER_DATABASE_CONNECTION=./data/aimeter.db
# Examples by engine:
# AIMETER_DATABASE_CONNECTION=./data/aimeter.db
# AIMETER_DATABASE_CONNECTION=mysql://root:password@127.0.0.1:3306/aimeter
# AIMETER_DATABASE_CONNECTION=postgresql://postgres:password@127.0.0.1:5432/aimeter

# Encrypt provider credentials stored in the main database.
# Allowed:
# - any non-empty string: used as the encryption secret
# Default when unset in database mode: auto-generated on first start and stored in the database
# Default when unset in env mode: startup will fail in production
# Override only when a fixed key is required (e.g. multiple instances sharing one database)
# Example:
# AIMETER_ENCRYPTION_KEY=

# Authentication
# Sign authentication session cookies.
# Allowed:
# - any non-empty string: used to sign session cookies
# Default when unset in database mode: auto-generated on first start and stored in the database
# Default when unset in env mode: random in-memory value (sessions invalidated on restart)
# Override only when a fixed secret is required (e.g. multiple instances sharing one database)
# Example:
# AIMETER_AUTH_SESSION_SECRET=

# Session cookie lifetime in seconds.
# Allowed:
# - any positive integer: session lifetime in seconds
# Built-in default when unset in both env and config.yaml: 43200 (12 hours)
AIMETER_AUTH_SESSION_TTL_SECONDS=43200

# Mark auth cookies as Secure.
# Allowed:
# - true: send auth cookies only over HTTPS
# - false: allow auth cookies over HTTP for local development
# Built-in default when unset in both env and config.yaml:
# - true in production
# - false in non-production
AIMETER_SECURE_COOKIE=true

# Read-only normal-entry password source when database mode is disabled.
# Allowed:
# - PBKDF2 hash string in format pbkdf2_sha256$<iterations>$<salt>$<digest>: enable normal-entry password verification from env/config
# Default when unset in both env and config.yaml: unset
# If unset in non-database mode, normal entry login cannot be completed
# Example:
# AIMETER_NORMAL_PASSWORD_HASH=pbkdf2_sha256$210000$replace-salt$replace-digest

# Read-only admin-entry password source when database mode is disabled.
# Allowed:
# - PBKDF2 hash string in format pbkdf2_sha256$<iterations>$<salt>$<digest>: enable admin-entry password verification from env/config
# Default when unset in both env and config.yaml: unset
# If unset in non-database mode, admin entry login cannot be completed
# Example:
# AIMETER_ADMIN_PASSWORD_HASH=pbkdf2_sha256$210000$replace-salt$replace-digest

# Admin route secret used for the management entry URL.
# Allowed:
# - exactly 32 random characters: admin UI is served from http://host:3000/<secret>
# Default when unset in both env and config.yaml: unset
# Recommended: generate a high-entropy random string and keep it private
# Example:
# AIMETER_ADMIN_ROUTE_PATH=replace-with-32-char-random-secret

# Shared secret for external cron/webhook refresh jobs (serverless friendly).
# Allowed:
# - any non-empty string: caller must send this value in X-AIMETER-CRON-SECRET
# Default when unset in both env and config.yaml: unset (cron endpoint disabled)
# Note: in database mode this is auto-generated and stored in the settings table; this env var is ignored.
# Example:
# AIMETER_CRON_SECRET=replace-with-strong-random-cron-secret

# Shared secret for external script access to /api/endpoint/subscriptions.
# Allowed:
# - any non-empty string: caller must send this value in X-AIMETER-ENDPOINT-SECRET
# Default when unset in both env and config.yaml: unset (secret-based access disabled)
# Note: in database mode this is auto-generated and stored in the settings table; this env var is ignored.
# Example:
# AIMETER_ENDPOINT_SECRET=replace-with-strong-random-endpoint-secret

# Antigravity OAuth
# OAuth client ID used for Antigravity Google token refresh.
# Allowed:
# - any non-empty OAuth client ID string
# Built-in default when unset: Antigravity official client ID
# Example:
# ANTIGRAVITY_OAUTH_CLIENT_ID=1071006060591-xxxxx.apps.googleusercontent.com

# OAuth client secret used for Antigravity code exchange and token refresh.
# Allowed:
# - any non-empty OAuth client secret string
# Required when using Antigravity OAuth link exchange or refresh flow.
# Example:
# ANTIGRAVITY_OAUTH_CLIENT_SECRET=replace-with-antigravity-client-secret

# Authentication rate limiting
# Rate-limit evaluation window length.
# Allowed:
# - any positive integer: window length in milliseconds
# Built-in default when unset in both env and config.yaml: 60000
AIMETER_AUTH_RATE_LIMIT_WINDOW_MS=60000

# Maximum allowed failed attempts within the rate-limit window.
# Allowed:
# - any positive integer: maximum failed attempts before blocking
# Built-in default when unset in both env and config.yaml: 5
AIMETER_AUTH_RATE_LIMIT_MAX_ATTEMPTS=5

# Temporary block duration after exceeding rate limit.
# Allowed:
# - any positive integer: block duration in milliseconds
# Built-in default when unset in both env and config.yaml: 300000
AIMETER_AUTH_RATE_LIMIT_BLOCK_MS=300000

# Advanced provider override (used only when database mode is disabled)
# When database mode is enabled, runtime provider data is loaded from the database.
# In that case, these overrides are ignored for runtime provider loading and are not merged with database providers.
# If set while database mode is disabled, it replaces config.yaml "providers" entirely.
# Allowed:
# - comma-separated provider IDs, for example cursor_main,openrouter_backup
# Default when unset: use providers from config.yaml
# Example:
# AIMETER_PROVIDER_IDS=cursor_main,openrouter_backup
#
# Per-provider variables use this pattern:
# AIMETER_PROVIDER__<ID>__<FIELD>
# Where <ID> is the uppercased provider ID with non-alphanumeric characters replaced by "_".
#
# Supported fields:
# TYPE
# Allowed:
# - aliyun: Aliyun account/provider
# - antigravity: Antigravity account/provider
# - claude: Claude account/provider
# - codex: Codex account/provider
# - cursor: Cursor subscription/provider
# - openrouter: OpenRouter account/provider
# - copilot: Copilot account/provider
# - minimax: MiniMax account/provider
# - kimi: Kimi account/provider
# - zai: Z.ai account/provider
# - ollama: Ollama local/provider endpoint
# - opencode: OpenCode account/provider
#
# NAME
# Allowed:
# - any string: UI display name for the provider entry
# Default when unset: unset
#
# AUTH_TYPE
# Allowed:
# - cookie: cookie string or cookie-based session payload
# - api_key: API key string
# - oauth: OAuth token payload or access token
# - jwt: JWT token string
# Built-in default when unset for a provider override: cookie
#
# CREDENTIAL
# Allowed:
# - any non-empty string: raw credential value expected by the selected AUTH_TYPE
# - for oauth providers (Claude/Codex/Copilot/Antigravity), this can be a plain access token string
#   or a JSON string containing access/refresh/client/expires bundle fields
# Default: none; required when provider override is used
#
# REFRESH_INTERVAL
# Allowed:
# - 0: disable scheduled refresh
# - any integer greater than 0: refresh interval in minutes
# Built-in default when unset for a provider override: 0
# 0 means no scheduled refresh in node mode
#
# REGION
# Allowed:
# - any provider-defined string, for example minimax_cn or zai_cn
# Default when unset: unset
#
# CLAUDE_AUTH_MODE
# Allowed:
# - cookie: use cookie-based Claude auth
# - oauth: use OAuth-based Claude auth
# Default when unset: unset
#
# OPENCODE_WORKSPACE_ID
# Allowed:
# - any string: OpenCode workspace ID
# Default when unset: unset
#
# Example provider overrides:
# AIMETER_PROVIDER__CURSOR_MAIN__TYPE=cursor
# AIMETER_PROVIDER__CURSOR_MAIN__NAME=Main Cursor
# AIMETER_PROVIDER__CURSOR_MAIN__AUTH_TYPE=cookie
# AIMETER_PROVIDER__CURSOR_MAIN__CREDENTIAL=replace-with-cursor-cookie
# AIMETER_PROVIDER__CURSOR_MAIN__REFRESH_INTERVAL=5
#
# AIMETER_PROVIDER__OPENROUTER_BACKUP__TYPE=openrouter
# AIMETER_PROVIDER__OPENROUTER_BACKUP__NAME=Backup OpenRouter
# AIMETER_PROVIDER__OPENROUTER_BACKUP__AUTH_TYPE=api_key
# AIMETER_PROVIDER__OPENROUTER_BACKUP__CREDENTIAL=replace-with-openrouter-api-key
# AIMETER_PROVIDER__OPENROUTER_BACKUP__REFRESH_INTERVAL=10

# Main file-based configuration example:
# See config.example.yaml
